import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Ranking",
  description: "Shared statistics on the game",
};

function Ranking() {
  return <h1>This is the ranking</h1>;
}
export default Ranking;
